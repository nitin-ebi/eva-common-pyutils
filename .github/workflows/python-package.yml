# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        mongodb-version: [ubuntu2204-6.0.6]
        mongotools_version : [ubuntu2204-x86_64-100.13.0]
        mongosh-version: [1.10.6]
        python-version: [3.8, 3.9]
        java-version: [1.11]

    steps:
    - uses: actions/checkout@v2
    - name: Set up MongoDB ${{ matrix.mongodb-version }} Mongosh ${{ matrix.mongosh-version }} and Mongo tools ${{ matrix.mongotools_version }} sharded environment
      run: sudo bash scripts/create_mongo_single_node_sharded_env.sh ${{ matrix.mongodb-version }} ${{ matrix.mongosh-version }} ${{ matrix.mongotools_version }}
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        pip install .[eva-internal]
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
        PYTHONPATH=. pytest -s tests
